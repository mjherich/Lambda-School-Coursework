const initialState = {
    userType: '',
    getError: '',
    postError: '',
    putError: '',
    isGetting: false,
    isPosting: false,
    isPutting: false,
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}